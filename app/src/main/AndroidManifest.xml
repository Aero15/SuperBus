<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SuperBus">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.SuperBus">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.details.StopDetailsActivity"
            android:exported="false"
            android:theme="@style/Theme.SuperBus" />
        <activity
            android:name=".ui.about.AboutActivity"
            android:exported="false"
            android:theme="@style/Theme.SuperBus" />
        <activity
            android:name=".ui.lines.LinesActivity"
            android:exported="false"
            android:theme="@style/Theme.SuperBus" />
        <activity
            android:name=".ui.linedetails.LineDetailsActivity"
            android:exported="false"
            android:theme="@style/Theme.SuperBus" />
        <activity
            android:name=".ui.details.velocite.VelociteDetailsActivity"
            android:exported="false"
            android:theme="@style/Theme.SuperBus" />

        <service
            android:name=".core.tts.TtsForegroundService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Continuation of bus arrival countdown and voice announcements while screen is off or app is in background." />
        </service>
    </application>
</manifest>